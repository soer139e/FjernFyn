<Window x:Class="fjernfyn.Views.SendResponseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:fjernfyn.Views"
        mc:Ignorable="d"
        Title="SendResponseWindow" Height="450" Width="800">
    <Grid>

        <Grid.Background>
            <ImageBrush ImageSource="../Assets/icons/bgicon.png" Stretch="Uniform" Opacity="0.25"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Titel" Grid.Row ="1" Grid.Column="0" Height="31" VerticalAlignment="Top" Margin="67,0,151,0"/>
        <Label Content="Indhold" Grid.Row ="1" Grid.Column="0" Height="31"  Margin="67,54,141,173" Grid.RowSpan="2"/>
        <TextBox Text ="{Binding Mail.Subject, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Margin="60,25,58,27"/>
        <TextBox Text="{Binding Mail.Body, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.ColumnSpan="2" Grid.RowSpan="1" Margin="60,0,58,0" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Visible"/>
        <Label Grid.Row="2" Grid.Column="2" Margin="33,147,33,10" FontSize="20"/>
        <Button Content="SendSvar" Grid.Column="2" Grid.Row="3" Margin="142,39,10,10" Width="115" Command="{Binding SendEmailCommand}"/>
    </Grid>
</Window>
